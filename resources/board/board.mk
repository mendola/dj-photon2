# List of all the board related files.
BOARDSRC = $(PROJ_ROOT)/resources/board/board.c

# Required include directories
BOARDINC = $(PROJ_ROOT)/resources/board

# Shared variables
ALLCSRC += $(BOARDSRC)
ALLINC  += $(BOARDINC)
